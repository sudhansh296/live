<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release: HTTPS only. Certificate pinning is added once the production domain is known. -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
