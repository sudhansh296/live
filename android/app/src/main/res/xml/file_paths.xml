<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the folder where camera shots for the profile photo are written. Nothing else is shared. -->
    <cache-path name="camera" path="camera/" />
</paths>
